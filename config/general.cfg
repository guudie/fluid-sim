// expected duration (in ms) between updates (ticks), or between two render calls
tick_duration = 16;

// number of physics calculations per tick
num_iterations = 4;

cell_size = 16;
gravity = {
    x = 0.0;
    y = 0.03;
};

// fluid properties
// lower K and higher viscosity makes the liquid behave more water-like
K = 2000.0;
h = 8.0;
p0 = 10.0;
viscosity = 1000.0;
mass = 1.0;
max_vel = 4.0;
max_acc = 0.7;
mouse_coeff = 0.01;